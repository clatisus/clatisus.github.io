# The URL the site will be built for
base_url = "https://clatis.us/"

# The site title and description; used in feeds by default.
title = "Junjie Wu"
description = "An ENFJ-T engineer, writes about programming, life, and everything in between."

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

taxonomies = [{ name = "tags" }]

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

highlight_theme = "OneHalfLight"

[extra]

# control dark mode: auto | dark | toggle
mode = "toggle"

# interal CDN resource
useCDN = false

favicon = "/icon/favicon.png"

copyright = "Junjie Wu"

ga = "G-C7NNSNC2Y1"

show_more = "Read more ⟶"
previous_page = "← Previous"
next_page = "Next →"
posted_on = "on "
posted_by = "Published by"
read_time = "minute read"

menus = [
    { name = "Home", url = "/" },
    { name = "Posts", url = "/posts" },
    { name = "About", url = "/about" },
    { name = "Tags", url = "/tags" },
]

# config social icon info in the footer
[[extra.social]]
icon = "github"
name = "GitHub"
url = "https://github.com/clatisus"

[[extra.social]]
icon = "linkedin"
name = "LinkedIn"
url = "https://www.linkedin.com/in/junjie-wu-867051166"

[[extra.social]]
icon = "mail"
name = "Email"
url = "mailto:clatisus@gmail.com"
